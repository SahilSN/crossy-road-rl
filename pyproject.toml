[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "crossy-road-rl"
version = "0.1.0"
dependencies = [
    "gymnasium",
    "numpy",
]

[tool.setuptools]
packages = ["crossyroad_rl"]
